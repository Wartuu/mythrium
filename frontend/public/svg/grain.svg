<svg>

    <filter id="noise">
        <feTurbulence type="fractalNoise" baseFrequency="0.1" numOctaves="2500" stitchTiles="stitch"/>
    </filter>

    <rect width="100%" height="100%" filter="url(#noise)"> 
</svg>